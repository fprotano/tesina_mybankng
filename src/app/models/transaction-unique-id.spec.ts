import { TransactionUniqueId } from './transaction-unique-id';

describe('TransactionUniqueId', () => {
  it('should create an instance', () => {
    expect(new TransactionUniqueId()).toBeTruthy();
  });
});

import { InternalTransaction } from './internal-transaction';

describe('InternalTransaction', () => {
  it('should create an instance', () => {
    expect(new InternalTransaction()).toBeTruthy();
  });
});

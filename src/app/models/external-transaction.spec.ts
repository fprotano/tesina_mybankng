import { ExternalTransaction } from './external-transaction';

describe('ExternalTransaction', () => {
  it('should create an instance', () => {
    expect(new ExternalTransaction()).toBeTruthy();
  });
});
